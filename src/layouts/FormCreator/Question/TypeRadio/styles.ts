import { styled } from 'src/styles/stitches.config';

export const RadioArea = styled('div', {
  width: '100%',
  display: 'flex',
  gap: '15px',
  alignItems: 'flex-start',
  position: 'relative',
});

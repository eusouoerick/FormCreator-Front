import { styled } from 'src/styles/stitches.config';

export const BtnAddOption = styled('button', {
  border: 'none',
  background: 'none',
  fontSize: '16px',
  marginTop: '15px',
  color: '$primary',
  cursor: 'pointer',
});
